<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <!-- Bagian Judul -->
    <title><%= statusCode %> : <%= message%></title>

    <!-- Bagian Icon -->
    <link rel="icon" href="./images/errorIcon.png" />

    <!-- Bagian CSS Sendiri -->
    <link rel="stylesheet" href="/css/error.css" />

    <!-- Bagian CSS Untuk Bootstrap -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
  </head>
  <body>
    <div class="container error d-flex align-items-center">
      <div
        class="row justify-content-center align-items-center h-75 position-relative"
      >
        <div class="col-sm-6">
          <img src="../../images/errorHero.jpg" alt="Error" class="w-100" />
        </div>
        <div class="col-sm-6">
          <div
            class="text-center d-flex flex-column justify-content-evenly px-5"
          >
            <h1 class="status_code"><%= statusCode %></h1>
            <p class="status_info fw-bold">
              <% if(statusCode !== 404) { %> Bad Request <%= message %> <% }
              else { %> <%= message %> <% } %>
            </p>
            <a href="/"
              ><button
                class="back-button px-4 py-2 rounded bg-danger text-light fw-bold border-0"
              >
                Go Back
              </button></a
            >
          </div>
        </div>
      </div>
    </div>

    <!-- Script JS Untuk Bootstrap -->
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
